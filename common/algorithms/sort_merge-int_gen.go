// This file was automatically generated by genny.
// Any changes will be lost if this file is regenerated.
// see https://github.com/cheekybits/genny

package algorithms

// ComparatorInt is a function that given two elements of type int returns x such that:
// x < 0 iff e1 < e2
// x > 0 iff e1 > e2
// x = 0 iff e1 = e2
type ComparatorInt func(e1, e2 int) int

func MergeSortInt(array []int, compare ComparatorInt) {
	b := append([]int(nil), array...) // shallow copy
	mergeSortInt(array, compare, 0, len(array)-1, b)
}

func mergeSortInt(array []int, compare ComparatorInt, low, high int, b []int) {
	if low >= high {
		return
	}

	middle := low + (high-low)/2
	mergeSortInt(array, compare, low, middle, b)
	mergeSortInt(array, compare, middle+1, high, b)
	mergeInt(array, compare, low, middle, high, b)
}

func mergeInt(array []int, compare ComparatorInt, low int, middle int, high int, b []int) {

	for i := low; i <= middle; i++ {
		b[i] = array[i]
	}
	for i := middle + 1; i <= high; i++ {
		b[i] = array[i]
	}

	lowQueue := low
	highQueue := middle + 1
	i := low
	for ; lowQueue <= middle && highQueue <= high; i++ {
		if compare(b[lowQueue], b[highQueue]) > 0 { // b[lowQueue] > b[highQueue]
			array[i] = b[highQueue]
			highQueue++
		} else {
			array[i] = b[lowQueue]
			lowQueue++
		}
	}

	for ; lowQueue <= middle; i++ {
		array[i] = b[lowQueue]
		lowQueue++
	}

	for ; highQueue <= high; i++ {
		array[i] = b[highQueue]
		highQueue++
	}
}
