// This file was automatically generated by genny.
// Any changes will be lost if this file is regenerated.
// see https://github.com/cheekybits/genny

package algorithms

// ComparatorFloat64 is a function that given two elements of type float64 returns x such that:
// x < 0 iff e1 < e2
// x > 0 iff e1 > e2
// x = 0 iff e1 = e2
type ComparatorFloat64 func(e1, e2 float64) int

func MergeSortFloat64(array []float64, compare ComparatorFloat64) {
	b := append([]float64(nil), array...) // shallow copy
	mergeSortFloat64(array, compare, 0, len(array)-1, b)
}

func mergeSortFloat64(array []float64, compare ComparatorFloat64, low, high int, b []float64) {
	if low >= high {
		return
	}

	middle := low + (high-low)/2
	mergeSortFloat64(array, compare, low, middle, b)
	mergeSortFloat64(array, compare, middle+1, high, b)
	mergeFloat64(array, compare, low, middle, high, b)
}

func mergeFloat64(array []float64, compare ComparatorFloat64, low int, middle int, high int, b []float64) {

	for i := low; i <= middle; i++ {
		b[i] = array[i]
	}
	for i := middle + 1; i <= high; i++ {
		b[i] = array[i]
	}

	lowQueue := low
	highQueue := middle + 1
	i := low
	for ; lowQueue <= middle && highQueue <= high; i++ {
		if compare(b[lowQueue], b[highQueue]) > 0 { // b[lowQueue] > b[highQueue]
			array[i] = b[highQueue]
			highQueue++
		} else {
			array[i] = b[lowQueue]
			lowQueue++
		}
	}

	for ; lowQueue <= middle; i++ {
		array[i] = b[lowQueue]
		lowQueue++
	}

	for ; highQueue <= high; i++ {
		array[i] = b[highQueue]
		highQueue++
	}
}
